location = $storeDebugPath {
  default_type application/json;
  return 200 '{"storeApiPrefix": "$storeApiPrefix", "storeRegionPublic": "$storeRegionPublic", "storeRegionSecret": "$storeRegionSecret", "storeRegionAuthenticated": "$storeRegionAuthenticated", "storeRegionManagers": "$storeRegionManagers", "storeRegionPerUser": "$storeRegionPerUser", "storeRegionPerOwner": "$storeRegionPerOwner", "authUserId": "$authUserId", "authUserToken": "$authUserToken", "userLocation": "$userLocation", "document_root": "$document_root", "uri": "$uri", "request_uri": "$request_uri", "remote_addr": "$remote_addr", "http_referer": "$http_referer"}';
}
